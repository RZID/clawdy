import 'package:flutter/material.dart';

class AppColors {
  // Brand Colors
  static const Color primary = Color(0xFF4F46E5); // Deep Indigo/Blue for "New Thesis" card
  static const Color accent = Color(0xFF3B82F6);  // Accent blue

  // Neutral Colors
  static const Color background = Color(0xFFFFFFFF);
  static const Color surface = Color(0xFFF9FAFB);     // Very light gray for card backgrounds
  static const Color cardBorder = Color(0xFFE5E7EB);   // Light gray for borders
  static const Color textPrimary = Color(0xFF111827);  // Dark gray/black for main text
  static const Color textSecondary = Color(0xFF6B7280); // Medium gray for descriptions
  static const Color textHint = Color(0xFF9CA3AF);      // Light gray for hints/placeholders

  // Chat Bubble / Special Elements
  static const Color userBubble = Color(0xFF0C0C0E);   // Dark black-ish bubble
  static const Color userBubbleText = Color(0xFFFFFFFF);
  static const Color inputBackground = Color(0xFFF3F4F6); // Gray background for chat input
  
  // Custom button/divider lines
  static const Color divider = Color(0xFFE5E7EB);
}
